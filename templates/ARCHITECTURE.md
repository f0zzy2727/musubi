# Architecture

<!-- Oya reads this so she can tell when a slice cuts against the structure the
     project is meant to have — the wrong layer, a bypassed boundary, a decision
     already settled being silently re-litigated in the diff. Keep it to the shape
     that matters; deep per-decision rationale belongs in docs/adr/. Delete this
     comment when filled. -->

**Last updated:** <ISO-8601 date>

---

## Overview

<!-- A few sentences (or an ASCII sketch) of the system's shape: the main pieces
     and how they fit together. Enough that a new reader knows where things live. -->

## Key components

<!-- The handful of parts that carry the design. One line each: what it does and
     where it lives. -->

| Component | Responsibility | Where |
|---|---|---|
| | | |

## Boundaries & constraints

<!-- The lines that must not be crossed: trust boundaries, tenant isolation, the
     "X must never call Y directly" rules. Oya watches for slices that breach these. -->

-
-

## Settled decisions

<!-- Architectural choices that are locked — record them here (or link the ADR) so
     they aren't quietly re-derived from the diff. -->

| Decision | Why | ADR / source |
|---|---|---|
| | | |
